import styled from 'styled-components'

import { BaseLabel } from '../Base'

const FormLabel = styled(BaseLabel)``

FormLabel.defaultProps = {
  display: 'block',
  fontWeight: 1,
  fontSize: 1,
  mb: 1,
}

export default FormLabel
